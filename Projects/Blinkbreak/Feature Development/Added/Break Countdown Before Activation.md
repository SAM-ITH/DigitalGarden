# Break Countdown Before Activation

we need to add feature that shows a 5-second countdown near the cursor just before the break overlay(reminderView) activates, so users are never caught off-guard. The countdown duration is configurable (5s or 10s). currently app shows no pre-break warning and the full-screen overlay appears abruptly.  configurable settings should appear in general view.