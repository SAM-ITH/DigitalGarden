# Snooze Postpone System

we need to offers +1 min, +5 min, and +15 min snooze options directly in the break reminder (replacing the old "Skip Break" button). Users can also configure keyboard shortcuts for snoozing. currently app only has a "Skip Break" (stop) button that immediately ends the break. configurable settings related to this feature should be in  general view.